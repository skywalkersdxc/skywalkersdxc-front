import React from "react";
import {FlightInfoComponentProps} from "../FlightCard/FlightInfoComponent";

const FlightDetail: React.FC<FlightInfoComponentProps> = ({itineraries, passengers}: FlightInfoComponentProps) => {
    return (
        <div></div>
    );
};

export default FlightDetail;
